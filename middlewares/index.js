const {
  addValidateBody,
  putValidateBody,
  patchValidateBody,
} = require("./validateBody");

const isValidId = require("./isValidId");

module.exports = {
  addValidateBody,
  putValidateBody,
  patchValidateBody,
  isValidId,
};
